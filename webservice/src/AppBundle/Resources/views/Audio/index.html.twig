{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Audio list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Number</th>
                <th>Description</th>
                <th>Saga</th>
                <th>Genre</th>
                <th>Author</th>
                <th>Uploadby</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('audios_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.name }}</td>
                <td>{{ entity.number }}</td>
                <td>{{ entity.description }}</td>
                <td>{{ entity.saga }}</td>
                <td>{{ entity.genre }}</td>
                <td>{{ entity.author }}</td>
                <td>{{ entity.uploadBy }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('audios_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('audios_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('audios_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
