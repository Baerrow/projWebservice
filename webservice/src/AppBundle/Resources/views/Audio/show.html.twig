{% extends '::base.html.twig' %}

{% block body -%}
    <h1>{{ entity.name }}</h1>

    <table class="record_properties table-striped table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ entity.name }}</td>
            </tr>
            <tr>
                <th>Number</th>
                <td>{{ entity.number }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ entity.description }}</td>
            </tr>
            <tr>
                <th>Saga</th>
                <td>{{ entity.saga }}</td>
            </tr>
            <tr>
                <th>Genre</th>
                <td>{{ entity.genre }}</td>
            </tr>
            <tr>
                <th>Author</th>
                <td>{{ entity.author }}</td>
            </tr>
            <tr>
                <audio id="player" src={{ entity.link }}></audio></th>
                <th>State</th>
                <td>
                    <button class="btn" onclick="document.getElementById('player').pauselol()">Play</button>
                    <button class="btn" onclick="document.getElementById('player').pause()">Pause</button>

                    
                </td> 
            </tr>
            <tr>
                <th>Uploadby</th>
                <td>{{ entity.uploadBy }}</td>
            </tr>
        </tbody>
    </table>

    <div class="btn">{{ form(delete_form) }}</div>

    <a href="{{ path('audios_edit' , { 'id': entity.id }) }}" > 
        <button class="btn">Editer</button>
    </a>

    <a href="{{ path('audios') }}" > 
        <button class="btn">Retour</button>
    </a>

    {#{{ form_label(form.name, 'Your Name', {'label_attr': {'class': 'foo'}}) }}#}

{% endblock %}

